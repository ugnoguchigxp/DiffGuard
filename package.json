{
  "name": "diffguard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./mcp/service": {
      "types": "./dist/mcp/service.d.ts",
      "default": "./dist/mcp/service.js"
    }
  },
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "bun": ">=1.2.0"
  },
  "bin": {
    "diffguard": "./dist/cli.js",
    "diffguard-mcp": "./dist/mcp/server.js"
  },
  "scripts": {
    "build": "rm -rf dist && bunx tsc -p tsconfig.build.json && bun scripts/fix-esm-imports.ts dist",
    "cli": "PATH=\"$HOME/.bun/bin:$PATH\" bun run src/cli.ts",
    "mcp": "PATH=\"$HOME/.bun/bin:$PATH\" bun run src/mcp/server.ts",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --write .",
    "test": "bunx vitest run --coverage",
    "test:watch": "bunx vitest",
    "test:coverage": "bunx vitest run --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/node": "^25.5.2",
    "@vitest/coverage-v8": "^4.1.3",
    "typescript": "^6.0.2",
    "vitest": "^4.1.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ts-morph": "^27.0.2",
    "zod": "^4.3.6"
  }
}
